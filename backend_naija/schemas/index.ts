//schema / for user.js

import user from './user';
import pin from './pin';
import save from './save';
import postedBy from './postedBy';
import comment from './comment';

export const schemaTypes = [user, postedBy, save, pin, comment]
